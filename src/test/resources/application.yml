spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

# 위의 설정은 default값 없어도 됨
# spring boot 테스트 에서는 별도의 세팅이 없을 경우 Default로 메모리 모드(DB)로 테스트를 돌린다.
